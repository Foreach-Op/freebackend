name: freebackend
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - django
  - mysqlclient
  - channels
  - channels_redis
prefix: /Users/oguzhantas/opt/anaconda3/envs/freebackend
